repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.6
    hooks:
      - id: ruff
        files: ^ml/src/
        args: ["check", "--fix"]

      - id: ruff-format
        files: ^ml/src/
